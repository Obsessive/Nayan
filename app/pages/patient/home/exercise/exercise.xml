<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="patientexLoaded">
  <!-- <ActionBar title="Nayan"> -->

        <Page.actionBar>
            <ActionBar>
                <ActionBar.text>
                    <Label text="Nayan &#xf06e;" android.position="left" class="action-bar"/>
                </ActionBar.text>
            </ActionBar>
        </Page.actionBar>

<ScrollView>

 	<ListView id="patientexlist" items="{{ patientexlist }}" itemTap="patientexlistitemTap">
		<ListView.itemTemplate>
		<!-- <StackLayout>
          <Label class="patienthomelabel" text="{{ name }}" class="patienthomecard"/>
          <Button class="doctorhomelabel" text="{{ name }}" tap="{{ listtapAction }}" />
        </StackLayout> -->
        <GridLayout rows="60" columns="*" marginTop="20" marginLeft="5" marginRight="5" class="" >
			<!-- <Image  col="0" row="0" src="{{ icon }}" stretch ="aspectFit" class="icon" /> -->
          <Label  col="0" row="0" class="patienthomecard" text="{{ name }}" />
          <!-- <Button class="patienthomelabel" text="{{ name }}" tap="{{ listtapAction }}" /> -->
        </GridLayout>

	</ListView.itemTemplate>
</ListView>
</ScrollView>
</Page>
