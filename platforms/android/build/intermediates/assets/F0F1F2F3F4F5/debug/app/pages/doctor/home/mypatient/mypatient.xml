<Page loaded="mypatientLoaded">
	<Page.actionBar>
            <ActionBar>
              <NavigationButton text="Go Back" android.systemIcon="ic_menu_back" tap="back"/>
            </ActionBar>
        </Page.actionBar>
<ScrollView>

 	<ListView id="mypatientlist" items="{{ mypatientlist }}" itemTap="mypatientlistitemTap">
		<ListView.itemTemplate>
		<StackLayout>
          <Label class="patientEyeConditionslabel" id="{{patientid}}" text="{{ name }}" />
          <!-- <Label text="{{ name }}" />  -->
          <!-- <Button class="patientEyeConditionslabel" text="{{ name }}" tap="{{ listtapAction }}" /> -->
        </StackLayout>
				<!-- <Label class="patientEyeConditionslabel" text="{{ name }}"  /> -->
			
	</ListView.itemTemplate>
</ListView>
</ScrollView>
</Page>